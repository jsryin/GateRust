{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "GateRust 客户端主窗口的最小权限集",
  "windows": ["main"],
  "permissions": [
    "core:app:allow-version",
    "core:window:allow-destroy",
    "core:window:allow-show"
  ]
}
